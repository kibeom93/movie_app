import React from "react";
import { func } from "prop-types";

function About () {
    return (
    <div className ="about__container">
    <span> About this page : I built it because i studied for my project.</span>
    </div>
    );
}

export default About; 